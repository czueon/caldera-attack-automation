# 프로젝트 아키텍처 설계 문서

## 📋 프로젝트 개요

### 목표
KISA TTPs 보고서를 기반으로 Caldera를 활용한 자동화된 사이버 공격 시뮬레이션 프레임워크 구축

### 핵심 사용자
기업 보안 담당자들이 KISA 보고서를 실제 환경에서 경험하고 학습할 수 있도록 지원

### 주요 특징
1. **보고서 추상화**: KISA 보고서를 MITRE ATT&CK 기반 Kill Chain으로 변환
2. **환경 자동 탐색**: Caldera Agent를 통해 실제 환경 정보 자동 수집
3. **MCP 기반 Ability 생성**: LLM이 환경에 맞는 공격 시나리오 자동 생성
4. **Self-Correcting**: 실행 실패 시 자동 수정 및 재시도 (목표: 90%+ 성공률)
5. **교육 중심**: 공격 흐름 이해 및 실패 과정 학습

---

## 🎯 프로젝트 차별점

### 기존 연구와의 비교

**기존 연구 (AURORA - Wang et al., 2024)**
```
CTI 보고서 → 시나리오 생성 → 실행 → 79% 성공률 → 수동 개입 필요
```

**우리 프로젝트 (Phase 1)**
```
KISA 보고서 → Goal 추출 → 환경 탐색 → MCP Ability 생성 →
실행 → Self-Correcting → 90%+ 성공률 (목표)

차별점:
1. Goal-Oriented Specification: 사용자 부담 최소화 (구체적 환경 정보 불필요)
2. Environment Discovery: Caldera Agent 기반 자동 환경 수집
3. Self-Correcting Attack Chain: 자동 수정 및 재시도 메커니즘
4. 교육 중심 설계: 실패 과정도 학습 자료로 활용
```

**우리 프로젝트 (Phase 2 - 장기)**
```
KISA 보고서 → Goal 추출 → AI가 환경 생성 (Docker/VM) →
MCP Ability 생성 → 실행 → Self-Correcting → 95%+ 성공률

추가 차별점:
5. Environment Generation: AI가 시뮬레이션 환경까지 자동 구성
6. One-Click Simulation: 원클릭으로 전체 프로세스 자동화
```

---

## 🏗️ 시스템 아키텍처

### 전체 워크플로우

```
┌────────────────────────────────────────────────────────────┐
│ INPUT: KISA 보고서 + 사용자 환경 (Caldera Agent 실행 중)     │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Phase 1: 보고서 분석 (Step 0-2)                             │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│ Step 0: PDF 파싱 (module0_pdf_processing.py)              │
│  → Raw text 추출                                           │
│                                                            │
│ Step 1: 개요 추출 (module1_overview_extraction.py)         │
│  → 시나리오 요약, 공격 상세, 흐름 추출                        │
│                                                            │
│ Step 2: 추상 공격 흐름 추출 (module2_abstract_flow.py) 🆕   │
│  → 환경 독립적 공격 목표만 추출                              │
│  → MITRE ATT&CK Tactics 매핑                               │
│                                                            │
│ Output: Abstract Attack Flow                               │
│ {                                                          │
│   attack_goals: [                                          │
│     {goal: "Gain initial access via web vulnerability",   │
│      tactic: "initial-access"},                           │
│     {goal: "Escalate privileges to SYSTEM",               │
│      tactic: "privilege-escalation"},                     │
│     ...                                                    │
│   ],                                                       │
│   mitre_tactics: ["initial-access", "execution", ...]     │
│ }                                                          │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Phase 2: 환경 정보 통합 (Step 3) 🆕                          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│ Step 3: 환경 정보 파싱 (module3_environment_integration.py) │
│  → 사용자가 작성한 environment_description.md 파싱          │
│  → 자연어 환경 설명을 구조화된 YAML로 변환                   │
│                                                            │
│ Input: templates/environment_description.md                │
│  (사용자가 간단한 자연어로 작성)                             │
│                                                            │
│  Example:                                                  │
│  ```                                                       │
│  ## 네트워크 구성                                           │
│  - 웹 서버(DMZ): 192.168.56.105 (Windows 10, IIS)         │
│  - 내부망: 192.168.56.106 (Windows 10, SMB)               │
│  - Caldera C2: 192.168.56.1:8888                          │
│                                                            │
│  ## 웹 애플리케이션                                         │
│  - 로그인: http://192.168.56.105/login_process.asp        │
│    - 방식: POST, 파라미터 userid, password                 │
│  - 계정: admin / P@ssw0rd!2020                             │
│                                                            │
│  ## 취약점                                                 │
│  1. ASP 파일 업로드 가능                                    │
│  2. PrintSpoofer 권한 상승 가능                             │
│  ```                                                       │
│                                                            │
│ Output: Structured Environment Config                      │
│ {                                                          │
│   network: {                                               │
│     systems: [                                             │
│       {name: "web_server", ip: "192.168.56.105",          │
│        os: "Windows 10", services: ["IIS"]},              │
│       {name: "internal_server", ip: "192.168.56.106",     │
│        os: "Windows 10", services: ["SMB"]}               │
│     ]                                                      │
│   },                                                       │
│   web_application: {                                       │
│     login: {url: "...", method: "POST",                   │
│             params: ["userid", "password"]},              │
│     credentials: {username: "admin",                      │
│                   password: "P@ssw0rd!2020"}              │
│   },                                                       │
│   vulnerabilities: [...],                                 │
│   payloads: ["cmd.asp", "PrintSpoofer64.exe", ...]        │
│ }                                                          │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Phase 3: 구체적 공격 흐름 생성 (Step 4) 🆕                    │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│ Step 4: 구체적 공격 흐름 생성 (module4_concrete_flow.py)    │
│  → Abstract Flow + Environment Config → Concrete Kill Chain│
│  → 환경 독립적 목표를 환경 특화 공격 단계로 변환              │
│                                                            │
│ Input:                                                     │
│  - Step 2의 Abstract Attack Flow                          │
│  - Step 3의 Environment Config                            │
│                                                            │
│ Output: Concrete Attack Flow (Kill Chain)                 │
│ {                                                          │
│   nodes: [                                                 │
│     {                                                      │
│       id: "node_001",                                      │
│       name: "Authenticate to Web Application",            │
│       tactic: "initial-access",                           │
│       technique: {id: "T1078", name: "Valid Accounts"},   │
│       environment_specific: {                             │
│         target: "192.168.56.105",                         │
│         url: "http://192.168.56.105/login_process.asp",   │
│         method: "POST",                                    │
│         params: ["userid", "password"],                   │
│         credentials: {username: "admin",                  │
│                       password: "P@ssw0rd!2020"}          │
│       }                                                    │
│     },                                                     │
│     ...                                                    │
│   ],                                                       │
│   edges: [                                                 │
│     {from: "node_001", to: "node_002",                    │
│      dependency_type: "required"}                         │
│   ],                                                       │
│   execution_order: ["node_001", "node_002", ...]          │
│ }                                                          │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Phase 4: 의존성 분석 및 실행 순서 (Step 5-6) ✅               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│ Step 5: 의존성 매트릭스 (module5_dependency_matrix.py)     │
│  → nodes와 edges 기반 dependency matrix 생성                │
│                                                            │
│ Step 6: 실행 순서 최적화 (module6_execution_order.py)      │
│  → Topological sort로 실행 순서 검증 및 최적화               │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Phase 5: Caldera Ability 생성 (Step 7) ✅                    │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│ Step 7: Ability YAML 생성 (module7_ability_generator.py)  │
│  → Concrete Flow의 environment_specific 정보를 활용         │
│  → 각 node를 Caldera ability YAML로 변환                   │
│                                                            │
│ Input: Step 4의 Concrete Attack Flow                      │
│                                                            │
│ Output: Caldera Ability YAML files                        │
│                                                            │
│ Example (node_001 → ability YAML):                        │
│ ```yaml                                                    │
│ id: uuid-generated                                         │
│ name: "Login to Web Application"                          │
│ description: "Authenticate using compromised credentials" │
│ tactic: "initial-access"                                  │
│ technique:                                                 │
│   attack_id: "T1078"                                      │
│   name: "Valid Accounts"                                  │
│ platforms: windows                                         │
│ executors:                                                 │
│   - name: psh                                              │
│     platform: windows                                      │
│     command: |                                             │
│       $cred = @{userid="admin"; password="P@ssw0rd!2020"} │
│       Invoke-WebRequest -Uri "http://192.168.56.105/..." \│
│         -Method POST -Body $cred                          │
│ ```                                                        │
│  - Target Network: 192.168.10.0/24                        │
│  - Available Exploit: printspoofer (Windows 10)           │
│  - Caldera Agent: Running on web-server-01                │
│                                                            │
│  Attack Chain:                                             │
│  1. Initial Access (T1190):                               │
│     Upload web shell to IIS server                        │
│  2. Execution (T1059):                                    │
│     Execute commands via web shell                        │
│  3. Privilege Escalation (T1068):                         │
│     Use printspoofer to gain SYSTEM privileges            │
│  4. Discovery (T1018):                                    │
│     Scan internal network 192.168.10.0/24                 │
│  5. Lateral Movement (T1021):                             │
│     Access discovered internal hosts                      │
│  6. Collection (T1005):                                   │
│     Collect sensitive data                                │
│  7. Exfiltration (T1041):                                 │
│     Exfiltrate data to external server"                   │
│                                                            │
│ Output: Environment-Aware MCP Prompt                       │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Phase 4: MCP Ability 생성 (Step 8)                         │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│ Caldera MCP Plugin 활용:                                   │
│ - LLM Ability Factory를 통한 Ability YAML 생성             │
│ - 환경에 맞는 구체적 명령어 생성                             │
│   * 실제 IP 주소: 192.168.10.50                           │
│   * 실제 경로: C:\inetpub\wwwroot                         │
│   * 실제 OS: Windows 10                                    │
│   * 실제 Executor: psh (PowerShell)                       │
│                                                            │
│ 생성되는 Ability 예시:                                      │
│ - upload_webshell.yml: IIS에 웹셸 업로드                   │
│ - execute_webshell.yml: 웹셸을 통한 명령 실행               │
│ - privesc_printspoofer.yml: printspoofer 권한 상승         │
│ - network_discovery.yml: 내부망 스캔                       │
│ - lateral_movement.yml: 내부 호스트 접근                   │
│ - data_collection.yml: 민감 데이터 수집                     │
│ - exfiltration.yml: 데이터 외부 유출                        │
│                                                            │
│ Output: Environment-Specific Caldera Abilities            │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Phase 5: 실행 + Self-Correcting (Step 9)                   │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│ 5-1. Caldera Operation 생성 및 실행                         │
│     - 생성된 Abilities를 Caldera에 업로드                   │
│     - Operation 생성 (Adversary profile)                   │
│     - Agent에서 순차 실행                                   │
│                                                            │
│ 5-2. 실행 모니터링                                          │
│     - Caldera API로 실시간 상태 확인                        │
│     - 각 Ability의 성공/실패 여부 추적                      │
│     - 실행 로그 (stdout, stderr) 수집                      │
│                                                            │
│ 5-3. Self-Correcting (MCP ReAct Pattern)                  │
│     실패 감지 → 원인 분석 → LLM 수정 → 재실행               │
│                                                            │
│     예시:                                                  │
│     [Iteration 1]                                          │
│     Thought: "Upload web shell to C:\inetpub\wwwroot"     │
│     Action: create_ability(upload_path="C:\inetpub\...")  │
│     Observation: "Access Denied"                           │
│                                                            │
│     [Iteration 2]                                          │
│     Thought: "Path may be restricted, try uploads folder" │
│     Action: create_ability(upload_path="C:\inetpub\...    │
│             \wwwroot\uploads")                             │
│     Observation: "Success"                                 │
│                                                            │
│     최대 5회 반복 (configurable)                            │
│                                                            │
│ 5-4. Fallback 전략                                         │
│     - 주요 실행 흐름을 해치지 않는 선에서 대안 시도           │
│     - 권한 상승 실패 → 다른 권한 상승 기법 시도              │
│     - 경로 오류 → 실제 경로 탐색 후 재시도                  │
│     - 네트워크 차단 → 다른 프로토콜/포트 시도                │
│                                                            │
│ 5-5. MLflow 로깅                                            │
│     - 각 iteration의 사고 과정 (thought_N)                 │
│     - 실행한 도구 및 인자 (tool_name_N, tool_args_N)       │
│     - 관찰 결과 (observation_N)                            │
│     - 최종 성공/실패 여부                                   │
│                                                            │
│ Output: Execution Results + Learning Data                  │
│ - 성공률: 90%+ (목표)                                       │
│ - 실패 원인 분석                                            │
│ - 수정 과정 로그                                            │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Phase 6: 결과 분석 및 교육 자료 생성 (Step 10)               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│ - 실행 결과 요약                                            │
│   * 성공한 단계                                             │
│   * 실패한 단계 및 원인                                     │
│   * Self-Correcting 과정                                   │
│                                                            │
│ - 보안 담당자를 위한 인사이트                                │
│   * 탐지 가능 지점 (Detection Points)                       │
│   * 대응 방안 (Mitigation Strategies)                      │
│   * 환경 강화 권고사항                                       │
│                                                            │
│ - 교육 자료                                                 │
│   * 공격 흐름 시각화                                        │
│   * MITRE ATT&CK 매핑                                      │
│   * 실패 사례를 통한 학습                                   │
│                                                            │
│ Output: Final Report + Educational Materials               │
└────────────────────────────────────────────────────────────┘
```

---

## 🔧 기술 스택

### 핵심 컴포넌트

1. **Caldera**
   - 버전: 5.3.0+
   - 역할: 공격 시뮬레이션 플랫폼
   - API: REST API v2

2. **Caldera MCP Plugin**
   - 역할: LLM-Caldera 통합
   - 기능:
     - LLM Ability Factory (Ability 자동 생성)
     - LLM Operation Planner (Operation 생성 및 실행)
     - ReAct Pattern (Self-Correcting)
   - 의존성: DSPy, MCP, MLflow

3. **LLM**
   - 기본 모델: GPT-4o (configurable)
   - 용도:
     - 보고서 파싱 및 분석
     - Kill Chain 생성
     - Ability 생성 (MCP를 통해)
     - Self-Correcting 로직

4. **환경 탐색 모듈** (신규 개발 필요)
   - Caldera API 연동
   - Agent 정보 수집
   - 정찰 Abilities 실행 및 결과 파싱

---

## 📁 프로젝트 구조

```
cyber_attack_analysis/
├── src/
│   ├── parsing/              # Step 1-2: 보고서 파싱
│   │   ├── report_parser.py
│   │   └── text_extractor.py
│   │
│   ├── killchain/            # Step 3-5: Kill Chain 생성
│   │   ├── killchain_generator.py
│   │   ├── dependency_matrix.py
│   │   └── execution_order.py
│   │
│   ├── environment/          # Step 6: 환경 탐색 (신규!)
│   │   ├── caldera_client.py      # Caldera API 연동
│   │   ├── agent_discovery.py     # Agent 정보 수집
│   │   ├── recon_executor.py      # 정찰 Abilities 실행
│   │   └── environment_mapper.py  # 환경 정보 구조화
│   │
│   ├── mcp/                  # Step 7-8: MCP 통합
│   │   ├── prompt_generator.py    # MCP 프롬프트 생성
│   │   ├── mcp_client.py          # MCP 플러그인 연동
│   │   └── ability_generator.py   # Ability 생성 래퍼
│   │
│   ├── execution/            # Step 9: 실행 및 Self-Correcting
│   │   ├── operation_manager.py   # Operation 생성 및 실행
│   │   ├── monitor.py             # 실행 모니터링
│   │   ├── self_correcting.py     # 자동 수정 로직
│   │   └── fallback_handler.py    # Fallback 전략
│   │
│   └── reporting/            # Step 10: 결과 분석
│       ├── result_analyzer.py
│       ├── report_generator.py
│       └── visualizer.py
│
├── abilities/                # 사전 준비된 Abilities
│   ├── recon/                # 정찰용 (환경 탐색)
│   │   ├── network_scan.yml
│   │   ├── service_discovery.yml
│   │   └── filesystem_discovery.yml
│   └── generated/            # MCP가 생성한 Abilities
│
├── config/
│   ├── caldera_config.yml    # Caldera 연결 설정
│   ├── mcp_config.yml        # MCP 설정
│   └── user_input.yml        # 사용자 추가 정보
│
├── data/
│   ├── reports/              # KISA 보고서 원본
│   ├── parsed/               # 파싱 결과
│   └── results/              # 실행 결과
│
└── docs/
    ├── project_architecture.md  # 이 문서
    ├── user_think.md            # 사용자 요구사항
    └── api_reference.md         # API 문서 (작성 예정)
```

---

## 🎓 교육적 가치

### 학습 목표

1. **공격 흐름 이해**
   - MITRE ATT&CK 기반 공격 단계
   - 각 단계의 목적과 기법

2. **실전 경험**
   - 실제 환경에서의 공격 시뮬레이션
   - 실패 사례를 통한 학습

3. **방어 관점 습득**
   - 탐지 가능 지점 식별
   - 대응 방안 학습

### 차별화된 교육 접근

- **실패도 학습 자료**: Self-Correcting 과정을 기록하여 실패 원인과 해결 과정 제공
- **맥락 이해**: 단순 공격 재현이 아닌, 보고서의 맥락을 유지하며 추상화
- **환경 독립성**: 구체적 환경이 달라도 동일한 시나리오 학습 가능

---

## 🔜 Phase 2: 환경 자동 생성 (장기 목표)

### 목표
사용자가 환경을 구성하지 않아도 AI가 자동으로 시뮬레이션 환경 생성

### 아키텍처

```python
def create_environment_from_goal(kisa_report):
    """
    KISA 보고서로부터 필요한 환경 요소를 추출하고 자동 생성
    """

    # 1. 보고서 분석 → 환경 요구사항 추출
    requirements = analyze_environment_requirements(kisa_report)
    # 예: "홈페이지를 통한" → 웹 서버 필요
    #     "내부망 장악" → 네트워크 분리 필요
    #     "Windows 10" → Win10 VM 필요

    # 2. Docker Compose / Terraform로 환경 생성
    infrastructure = {
        "web_server": {
            "image": "windows/servercore:ltsc2022",
            "services": ["IIS"],
            "vulnerabilities": ["file_upload"],
            "network": "dmz"
        },
        "internal_server": {
            "image": "mcr.microsoft.com/windows:10",
            "network": "internal",
            "data": ["sensitive_documents"]
        },
        "network": {
            "dmz": "192.168.10.0/24",
            "internal": "192.168.20.0/24"
        }
    }

    # 3. 환경 프로비저닝
    docker_compose = generate_docker_compose(infrastructure)
    deploy_environment(docker_compose)

    # 4. Caldera Agent 자동 배포
    for container in containers:
        deploy_caldera_agent(container)

    # 5. 취약점 설정
    setup_vulnerabilities(infrastructure["vulnerabilities"])

    return Environment(
        web_server_ip="192.168.10.10",  # 자동 할당
        internal_network="192.168.20.0/24",  # 자동 생성
        agents=[agent1, agent2],  # 자동 배포
        ready=True
    )
```

### 기술 요소
- Docker / Docker Compose
- Terraform (선택적)
- Caldera Agent 자동 배포 스크립트
- 취약점 자동 설정 (vulnerable by design)

---

## 📊 성공 지표

### Phase 1 (현재 프로젝트)
- ✅ KISA 보고서 파싱 정확도: 95%+
- ✅ Kill Chain 생성 정확도: 90%+
- ✅ 환경 탐색 성공률: 95%+
- ✅ Ability 생성 성공률: 85%+
- ✅ **시뮬레이션 최종 성공률: 90%+** (목표)
- ✅ Self-Correcting 평균 반복 횟수: 2-3회

### Phase 2 (장기)
- 환경 자동 생성 성공률: 95%+
- 전체 프로세스 자동화율: 100%
- 사용자 개입 필요 횟수: 0-1회
- 시뮬레이션 최종 성공률: 95%+

---

## 🚧 현재 상태 및 다음 단계

### 완료된 부분 (Step 1-5)
- ✅ 보고서 파싱
- ✅ Kill Chain 생성
- ✅ Dependency Matrix
- ✅ Execution Order
- ✅ Adversary 생성

### 진행 중 (Step 6)
- 🔄 환경 탐색 모듈 설계
- 🔄 Caldera API 연동

### 다음 단계
1. Step 6 구현: Environment Discovery
   - Caldera API 클라이언트 개발
   - 정찰 Abilities 작성
   - 환경 정보 구조화

2. Step 7-8 구현: MCP 통합
   - MCP 프롬프트 생성기
   - MCP 플러그인 연동
   - Ability 생성 테스트

3. Step 9 구현: Self-Correcting
   - 실행 모니터링
   - 실패 감지 및 분석
   - 자동 수정 로직

4. Step 10 구현: 결과 분석
   - 리포트 생성
   - 교육 자료 제작
   - 시각화

---

## 📚 참고 자료

### MITRE ATT&CK
- Tactics: https://attack.mitre.org/tactics/enterprise/
- Techniques: https://attack.mitre.org/techniques/enterprise/

### Caldera
- 공식 문서: https://caldera.readthedocs.io/
- REST API: https://caldera.readthedocs.io/en/latest/REST-API.html
- GitHub: https://github.com/mitre/caldera

### Caldera MCP Plugin
- GitHub: https://github.com/mitre/MCP
- Medium 소개글: https://medium.com/@mitrecaldera/connecting-llms-to-caldera-with-the-mcp-plugin-*

### 기존 연구
- AURORA: Wang et al., 2024 (79% 성공률)

---

**문서 작성일**: 2025-11-18
**최종 수정일**: 2025-11-18
**작성자**: Project Team
