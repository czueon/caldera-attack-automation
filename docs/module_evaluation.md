# 모듈 평가 및 분석

**평가일**: 2025-11-18
**평가 대상**: `C:\Users\user\Documents\cyber_attack_analysis\test\modules`

---

## 📋 현재 구현 현황

### 구현된 모듈 (10개)

| 모듈 | 파일 | 크기 | 상태 | 평가 |
|------|------|------|------|------|
| **Config** | `config.py` | 538B | ✅ 완료 | 기본 설정 |
| **Step 0** | `module0_pdf_processing.py` | 3.2KB | ✅ 완료 | PDF 파싱 |
| **Step 1** | `module1_overview_extraction.py` | 14.3KB | ✅ 완료 | 개요 추출 |
| **Step 1.5** | `module2_environment_requirements.py` | 12.8KB | ✅ 완료 | 환경 요구사항 |
| **Step 2** | `module3_killchain_generation.py` | 10.9KB | ✅ 완료 | Kill Chain 생성 |
| **Step 3.5** | `module3_5_attack_enhancement.py` | 12.4KB | ✅ 완료 | ATT&CK 강화 |
| **Step 4** | `module4_branch_generation.py` | 12.2KB | ✅ 완료 | 분기 생성 |
| **Step 5** | `module5_visualization.py` | 6.5KB | ✅ 완료 | 시각화 |
| **Step 6** | `module6_ability_generator.py` | 17.7KB | ✅ 완료 | Ability 생성 |
| **Step 6.5** | `module6_5_ability_splitter.py` | 5.3KB | ✅ 완료 | Ability 분할 |

---

## ✅ **강점 (Strengths)**

### 1. **체계적인 단계별 구조**
```
Step 0 → Step 1 → Step 1.5 → Step 2 → Step 3.5 → Step 4 → Step 5 → Step 6 → Step 6.5
```
- 명확한 파이프라인 구조
- 각 단계의 입출력이 YAML로 명확하게 정의됨
- 단계별 독립 실행 가능 (테스트 용이)

### 2. **LLM 활용 설계**
- Anthropic Claude API를 적극 활용
- 보고서 파싱, Kill Chain 생성, ATT&CK 매핑 등 복잡한 분석에 AI 사용
- 프롬프트 엔지니어링이 잘 되어 있음 (JSON/YAML 출력 유도)

### 3. **MITRE ATT&CK 통합**
- `module3_5_attack_enhancement.py`에서 ATT&CK 정보 자동 매핑
- Technique ID, 상세 설명, Prerequisites, Results 생성
- MITRE 라이브러리 (`mitreattack-python`) 활용

### 4. **Caldera Ability 생성 품질**
- `module6_ability_generator.py`가 매우 상세함
- Debugging-friendly 명령어 생성 (verbose output, 파일 저장)
- Error handling 포함
- Payload 자동 추출
- 환경 변수 검증

### 5. **환경 요구사항 추상화**
- `module2_environment_requirements.py`에서 "simplification strategies" 개념 도입
- 복잡한 공격을 목적(purpose) 중심으로 단순화
- Caldera 에이전트 전제조건 명확히 설정 (이미 실행 중)

---

## ⚠️ **현재 설계와의 차이점 (Gaps)**

### 1. **환경 탐색 모듈 부재 ⭐ 핵심!**

**설계 문서 (project_architecture.md):**
```
Phase 2: 환경 탐색 (Step 6)
- Caldera API를 통한 자동 환경 수집
- 정찰 Abilities 실행
- 사용자 추가 정보 입력
```

**현재 구현:**
- ❌ 환경 탐색 모듈이 없음
- ❌ Caldera API 연동 없음
- ❌ 실제 환경 정보를 가져오는 로직 없음

**문제점:**
```python
# module6_ability_generator.py에서 환경 변수를 사용하지만...
self.environment_vars = required_vars + optional_vars  # 단순 변수 이름만 로드

# 실제 환경 값은 어디서 오는가?
# 예: #{INTERNAL_HOMEPAGE_URL}, #{TEMP_DIR} 등의 실제 값은?
```

**현재 방식:**
- Step 1.5 (environment_requirements)에서 **변수 이름과 설명**만 생성
- **실제 환경 값(IP, 경로 등)**은 사용자가 수동으로 어딘가에 입력해야 함 (어디에? 불명확)
- Ability 생성 시 `#{VARIABLE}` placeholder만 사용

**설계 문서가 제안한 방식:**
1. Caldera API로 현재 연결된 Agent 정보 자동 수집 → IP, OS, hostname 획득
2. 정찰 Abilities 실행 → 웹 서버, 네트워크, 서비스 탐색
3. 수집된 정보를 구조화 → `environment_info.yml`
4. 사용자가 추가 정보 입력 (exploit 이름 등)
5. MCP 프롬프트 생성 시 **실제 환경 정보** 포함

**결론:**
- **현재 구현은 Step 6 이전까지만 완료됨**
- **환경과의 연결 고리가 끊어져 있음**
- Ability는 생성되지만, **실제 환경에서 실행 가능한지 불명확**

---

### 2. **MCP 통합 부재**

**설계 문서:**
```
Phase 4: MCP Ability 생성 (Step 8)
- Caldera MCP Plugin 활용
- LLM Ability Factory를 통한 자연어 기반 생성
- ReAct Pattern으로 Self-Correcting
```

**현재 구현:**
- ✅ `module6_ability_generator.py`가 LLM으로 Ability 생성 (자체 구현)
- ❌ MCP Plugin 사용하지 않음
- ❌ Self-Correcting 없음

**차이점:**
| 항목 | 현재 구현 | 설계 문서 (MCP) |
|------|----------|----------------|
| Ability 생성 | Claude API 직접 호출 | MCP Tool 호출 |
| Self-Correcting | 없음 | ReAct Pattern (최대 5회 반복) |
| Caldera 연동 | 없음 (YAML 파일만 생성) | MCP가 Caldera API 직접 호출 |
| 실행 모니터링 | 없음 | MLflow 로깅 및 관찰 |

**장단점:**
- **현재 방식의 장점:**
  - 구현이 간단함
  - MCP 의존성 없음
  - 생성된 YAML을 수동으로 검토 가능

- **MCP 방식의 장점:**
  - 자동으로 Caldera에 업로드
  - 실행 결과 확인 후 자동 수정
  - 높은 성공률 (Self-Correcting)
  - 교육적 가치 (실패 과정 학습)

---

### 3. **Self-Correcting 메커니즘 부재**

**설계 문서:**
```
Phase 5: 실행 + Self-Correcting (Step 9)
- Caldera Operation 생성 및 실행
- 실행 모니터링
- 실패 감지 → 원인 분석 → LLM 수정 → 재실행
- Fallback 전략
```

**현재 구현:**
- ❌ 실행 모듈 없음
- ❌ Self-Correcting 로직 없음
- ❌ Caldera API 호출 없음

**현재는 YAML 파일만 생성하고 끝:**
```
module6 output:
- abilities.yml
- adversary_profile.yml

→ 사용자가 수동으로 Caldera에 업로드해야 함
→ 실행 결과를 확인하고 수정하는 것은 사용자 몫
```

---

### 4. **결과 분석 및 교육 자료 생성 부재**

**설계 문서:**
```
Phase 6: 결과 분석 및 교육 자료 생성 (Step 10)
- 실행 결과 요약
- 보안 담당자를 위한 인사이트
- 교육 자료 (공격 흐름 시각화, 실패 사례 학습)
```

**현재 구현:**
- ✅ `module5_visualization.py` (시각화는 있음)
- ❌ 실행 결과 분석 없음
- ❌ 교육 자료 생성 없음
- ❌ Detection Points, Mitigation Strategies 제안 없음

---

## 🎯 **설계 문서 대비 완성도**

### 완성도 매트릭스

| Phase | 설계 문서 Step | 현재 구현 | 완성도 | 비고 |
|-------|--------------|----------|--------|------|
| **Phase 1** | Step 0-5 | ✅ 완료 | **100%** | 보고서 파싱 ~ Kill Chain 생성 |
| **Phase 2** | Step 6 (환경 탐색) | ❌ 없음 | **0%** | **핵심 누락** |
| **Phase 3** | Step 7 (프롬프트 생성) | ⚠️ 부분 | **50%** | 환경 정보 없이 생성 |
| **Phase 4** | Step 8 (MCP Ability) | ⚠️ 대체 | **70%** | MCP 대신 자체 구현 |
| **Phase 5** | Step 9 (Self-Correcting) | ❌ 없음 | **0%** | 실행 모듈 자체가 없음 |
| **Phase 6** | Step 10 (결과 분석) | ❌ 없음 | **0%** | 교육 자료 생성 없음 |

**전체 완성도: 약 37% (Phase 1만 완전 완성)**

---

## 📝 **현재 워크플로우 vs 설계 문서**

### 현재 실제 워크플로우:

```
┌────────────────────────────────────────────────────────────┐
│ INPUT: KISA 보고서 PDF                                       │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 0: PDF Processing                                     │
│ - PDF 텍스트 추출 (page-by-page)                            │
│ Output: step0_parsed.yml                                   │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 1: Overview Extraction                                │
│ - 개요 섹션 추출 (AI)                                        │
│ - 공격 상세 정보 추출 (AI)                                   │
│ - 시간순 재정렬 (AI)                                         │
│ - 시나리오 흐름 생성 (AI)                                    │
│ Output: step1_overview.yml                                 │
│   - overview_text, attack_details, scenario_flow          │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 1.5: Environment Requirements                         │
│ - 환경 요구사항 추출 (AI)                                    │
│ - Simplification Strategies 생성 (복잡한 공격 단순화)        │
│ - Environment Variables 정의 (#{VARIABLE} 형태)            │
│ Output: step1_5_environment.yml                            │
│   - environment_variables: {VAR: {desc, example, required}}│
│   - simplification_strategies: {action: {original, ...}}   │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 2: Kill Chain Generation                              │
│ - Scenario Graph 생성 (AI)                                  │
│   * Nodes: 공격 단계 (simplification strategies 기반)       │
│   * Edges: 단계 간 의존성                                    │
│ - Kill Chain Paths 찾기 (DFS)                              │
│ Output: step2_killchain.yml                                │
│   - scenario_graph: {nodes, edges}                         │
│   - kill_chains: [{chain_id, path, node_ids}]             │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 3.5: ATT&CK Enhancement                               │
│ - 각 노드에 MITRE ATT&CK 정보 추가 (AI)                     │
│   * technique_id: T1078.001                                │
│   * technique_name: Valid Accounts: Default Accounts       │
│   * description: 5-10줄 상세 설명                           │
│   * prerequisites: [{type, description}]                   │
│   * results: [{type, description}]                         │
│ Output: step3_5_enhanced.yml                               │
│   - enhanced_graph: {nodes (with ATT&CK), edges}           │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 4: Branch Generation                                  │
│ - 대체 공격 경로 추가 (AI)                                   │
│   * 같은 목적, 다른 방법 (예: keylogger vs clipboard)       │
│ - 여러 Kill Chain 생성                                      │
│ Output: step4_branches.yml                                 │
│   - enhanced_graph: {nodes (더 많음), edges (더 많음)}       │
│   - kill_chains: [chain-1, chain-2, ...]                  │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 5: Visualization                                      │
│ - Kill Chain 그래프 시각화 (Graphviz)                       │
│ Output: killchain_graph.png                                │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 6: Ability Generator                                  │
│ - 각 노드 → Caldera Ability YAML 변환 (AI)                 │
│   * Debugging-friendly 명령어                               │
│   * Error handling                                         │
│   * Payload 자동 추출                                        │
│   * 환경 변수 검증 (#{VAR} 문법)                            │
│ - Adversary Profile 생성                                    │
│ Output:                                                     │
│   - abilities.yml (Stockpile 형식)                         │
│   - adversary_profile.yml                                  │
└────────────────────────────────────────────────────────────┘
                   ↓
                ⚠️ 여기서 끝!
                사용자가 수동으로:
                1. Caldera에 abilities.yml 업로드
                2. #{VARIABLE} 값들을 어딘가에 설정 (?)
                3. Adversary 실행
                4. 결과 확인
```

### 설계 문서 워크플로우:

```
(Step 0-5는 동일)
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 6: Environment Discovery ⭐ (현재 없음!)                │
│ - Caldera API로 Agent 정보 수집                             │
│ - 정찰 Abilities 실행 (network, service discovery)         │
│ - 환경 정보 구조화                                           │
│   * IP: 192.168.10.50                                      │
│   * OS: Windows 10                                          │
│   * Web Server: IIS at C:\inetpub\wwwroot                 │
│   * Network: 192.168.10.0/24                               │
│ - 사용자 추가 정보 입력 (printspoofer 등)                   │
│ Output: environment_info.yml (실제 환경 값!)                │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 7: Environment-Aware Prompt Generation                │
│ - Abstract Kill Chain + Concrete Environment              │
│ - MCP 프롬프트 생성 (실제 IP, 경로 포함)                    │
│ Output: mcp_prompt.txt                                     │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 8: MCP Ability Generation                             │
│ - MCP Tool: create_ability                                 │
│ - 환경에 맞는 구체적 명령어 생성                             │
│   * 192.168.10.50 (실제 IP)                               │
│   * C:\inetpub\wwwroot (실제 경로)                         │
│ Output: Caldera에 바로 업로드됨                             │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 9: Execution + Self-Correcting                        │
│ - Caldera Operation 생성 및 실행                            │
│ - 실시간 모니터링                                            │
│ - 실패 감지 → MCP ReAct → 수정 → 재실행 (최대 5회)          │
│ - MLflow 로깅 (thought, action, observation)               │
│ Output: execution_results.yml (성공률 90%+ 목표)            │
└──────────────────┬─────────────────────────────────────────┘
                   ↓
┌────────────────────────────────────────────────────────────┐
│ Step 10: Result Analysis & Educational Materials          │
│ - 실행 결과 분석                                             │
│ - Detection Points 식별                                     │
│ - Mitigation Strategies 제안                               │
│ - 교육 자료 생성 (공격 흐름, 실패 사례)                      │
│ Output: final_report.pdf                                   │
└────────────────────────────────────────────────────────────┘
```

---

## 🚨 **핵심 문제점**

### 1. **환경 정보의 공백 (The Missing Link)**

**현재 상황:**
```yaml
# step1_5_environment.yml에서 생성
environment_variables:
  INTERNAL_HOMEPAGE_URL:
    description: "내부 홈페이지 URL"
    example: "http://192.168.1.100/board"
    required: true

# step6 abilities.yml에서 사용
command: |
  Invoke-WebRequest -Uri "#{INTERNAL_HOMEPAGE_URL}/login.asp" ...
```

**문제:**
- `#{INTERNAL_HOMEPAGE_URL}`의 **실제 값**은 어디에?
- 사용자가 어디에 입력해야 하는지 불명확
- Caldera가 이 값을 어디서 가져오는지 불명확

**해결 필요:**
1. **Option A (설계 문서 방식)**:
   - Caldera Agent로 자동 수집 → `192.168.10.50`
   - 사용자 입력 → `http://192.168.10.50/board`
   - `environment_info.yml` 파일에 **실제 값** 저장
   - Ability 생성 시 placeholder 대체

2. **Option B (Caldera Facts 활용)**:
   - Caldera의 Facts 시스템 활용
   - Agent가 수집한 정보를 Facts로 저장
   - Ability가 Facts를 참조
   - 예: `#{host.internal.ip}` → Caldera가 런타임에 대체

3. **Option C (수동 구성 파일)**:
   - 사용자가 `config.yml` 작성
   - Ability 생성 시 config 읽어서 대체
   - 간단하지만 자동화 부족

---

### 2. **실행 및 검증 부재**

**현재:** YAML 파일만 생성 → 끝

**필요:**
- Caldera API 연동
- Operation 생성 및 실행
- 결과 모니터링
- 성공/실패 여부 확인

**이것이 없으면:**
- 생성된 Ability가 실제로 작동하는지 알 수 없음
- Self-Correcting 불가능
- 교육적 가치 감소 (실행 과정 학습 불가)

---

### 3. **Self-Correcting 부재 = 차별점 약화**

**설계 문서의 핵심 차별점:**
> "기존 연구는 79% 성공률 + 수동 개입 필요
> 우리는 90%+ 성공률 + Self-Correcting 자동 수정"

**현재 구현:**
- Ability 생성은 잘 됨
- 하지만 실행 안 함 → 성공률 측정 불가
- Self-Correcting 없음 → 차별점 없음

---

## 💡 **개선 방향 제안**

### 단기 (프로젝트 완성)

**우선순위 1: 환경 연결 고리 구축**
```python
# 신규 모듈: module6_environment_discovery.py
def discover_environment():
    """
    Caldera API로 현재 환경 수집
    """
    # 1. Caldera API 연동
    caldera = CalderaClient("http://localhost:8888")
    agents = caldera.get_agents()

    # 2. Agent 정보 추출
    for agent in agents:
        environment_info = {
            "ip": agent.host_ip,
            "os": agent.platform,
            "hostname": agent.host
        }

    # 3. 정찰 Abilities 실행
    recon_results = caldera.run_recon(agent.paw)

    # 4. 환경 정보 구조화
    save_environment_info(environment_info)
```

**우선순위 2: Ability 생성 시 환경 정보 주입**
```python
# module6 수정
def generate_abilities(killchain, environment_info):
    """
    환경 정보를 활용한 Ability 생성
    """
    for node in killchain.nodes:
        # 환경 변수 대체
        prompt = f"""
        Environment:
        - IP: {environment_info['ip']}
        - OS: {environment_info['os']}
        - Web Root: {environment_info['web_root']}

        Create ability for: {node.name}
        """
```

**우선순위 3: 기본 실행 모듈**
```python
# 신규 모듈: module7_execution.py
def execute_operation(abilities, adversary):
    """
    Caldera Operation 실행
    """
    caldera = CalderaClient()

    # 1. Abilities 업로드
    for ability in abilities:
        caldera.upload_ability(ability)

    # 2. Operation 생성
    op = caldera.create_operation(adversary)

    # 3. 실행 및 모니터링
    results = caldera.run_and_monitor(op)

    # 4. 결과 분석
    return analyze_results(results)
```

### 중기 (MCP 통합)

**MCP Plugin 통합**
- 현재 `module6` 로직을 MCP로 마이그레이션
- Self-Correcting 구현
- MLflow 로깅

### 장기 (Phase 2)

**환경 자동 생성**
- Docker/VM 자동 프로비저닝
- 완전 자동화

---

## 📊 **종합 평가**

### ✅ 잘된 점
1. **보고서 분석 파이프라인 완성도 높음** (Step 0-5)
2. **LLM 활용 우수** (프롬프트 엔지니어링)
3. **ATT&CK 통합 훌륭함** (Step 3.5)
4. **Ability 생성 품질 우수** (Step 6)
5. **코드 구조 깔끔함** (모듈화, 독립 실행)

### ⚠️ 개선 필요
1. **환경 탐색 모듈 부재** (핵심!)
2. **실행 모듈 부재** (검증 불가)
3. **Self-Correcting 부재** (차별점 약화)
4. **Caldera API 연동 없음** (수동 업로드)
5. **교육 자료 생성 없음**

### 🎯 완성도
- **보고서 → Ability 생성**: 90% 완성
- **전체 프레임워크**: 37% 완성
- **설계 문서 대비**: Phase 1만 완료

---

## 🚀 **다음 단계 권장사항**

### 즉시 구현 (1주 이내)
1. ✅ **Caldera API 클라이언트 구현**
   - `modules/caldera_client.py`
   - `get_agents()`, `run_ability()`, `create_operation()`

2. ✅ **환경 탐색 모듈 구현**
   - `modules/module6_environment_discovery.py`
   - Agent 정보 수집 + 정찰 Abilities 실행

3. ✅ **환경 정보 주입**
   - `module6_ability_generator.py` 수정
   - 실제 환경 값으로 placeholder 대체

### 중기 구현 (2주 이내)
4. ✅ **실행 모듈 구현**
   - `modules/module7_execution.py`
   - Operation 생성 및 실행, 결과 모니터링

5. ✅ **기본 Self-Correcting**
   - 실패 감지 → 재생성 → 재실행 (1-2회)

### 장기 구현 (4주 이내)
6. ⭐ **MCP 통합**
   - MCP Plugin 활용
   - ReAct Pattern Self-Correcting

7. ⭐ **교육 자료 생성**
   - 결과 분석 리포트
   - Detection Points, Mitigation 제안

---

**총평**: 현재 구현은 "보고서 분석 → Ability 생성"까지는 매우 훌륭하나, "환경과의 연결" 및 "실행/검증" 부분이 누락되어 있어 실용성이 제한적입니다. **환경 탐색 모듈 구현이 가장 시급합니다.**
